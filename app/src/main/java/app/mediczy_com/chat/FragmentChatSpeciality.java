package app.mediczy_com.chat;


import android.support.v4.app.Fragment;

/**
 * Created by Prithivi on 17-03-2017.
 */

public class FragmentChatSpeciality extends Fragment {

/*    Context context;

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {
        View rootView = inflater.inflate(R.layout.fragment_chat_specality, container, false);
        context = getActivity();
        init(rootView);

        return rootView;
    }

    private void init(View rootView) {

        String displayName = LocalStore.getInstance().getFirstName(context);
        String userId = LocalStore.getInstance().getUserID(context);
 //       ApplozicBridge.launchIndividualChat(context, userId, displayName, "");
    }*/
}
